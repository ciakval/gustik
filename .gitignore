# Claude local settings
.claude/settings.local.json

# BMAD session memory
_bmad-output/**/.memlog.md

# OpenWolf ephemeral/local machine state (churns every session, no cross-session value)
.wolf/token-ledger.json
.wolf/_scan-state.json
.wolf/hooks/_session.json

# git worktrees for isolated per-story implementation (superpowers:using-git-worktrees)
.worktrees/
