[project]
name = "gustik-scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jan Remes", email = "ciakval@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "smbus2",
]

[project.scripts]
gustik-scripts = "gustik_scripts:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
